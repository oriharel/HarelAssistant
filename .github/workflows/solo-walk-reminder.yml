name: Solo Walk Reminder

on:
  schedule:
    # 14:00 UTC = 16:00 Israel Standard Time (UTC+2)
    # Note: Will be 17:00 during Israel Summer Time (UTC+3)
    - cron: '0 14 * * *'
  workflow_dispatch: # Allow manual run from GitHub UI

jobs:
  trigger_reminder:
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - name: Send WhatsApp reminder
        run: |
          curl -X POST "https://vitamin-reminder.vercel.app/api/whatsapp" \
            -H "Content-Type: application/json" \
            -d '{"message":"🐕 Time to walk Solo! 🐕"}'
